# Daily-Quote
A simple Quote of the Day web application created using HTML, CSS, and JavaScript. It fetches random quotes from a public API using the Fetch API. Users can generate a new quote with a button click. The app also allows sharing quotes directly on Twitter.
